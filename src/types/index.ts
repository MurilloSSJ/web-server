export type CredentialsType = {
    access_token: string;
    refresh_token: string;
    expires_in: number;
    token_type: string;
    scope: string;
    refresh_expires_in: number;
    id_token: string;
    session_state: string;
}